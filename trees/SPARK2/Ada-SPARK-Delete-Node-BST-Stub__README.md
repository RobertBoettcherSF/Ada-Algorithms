# Ada-SPARK-Delete-Node-BST-Stub

A bounded SPARK implementation of delete values from a bounded bst model.

## Verification

```text
make test
make prove
```

The package uses `SPARK_Mode => On`; proof is run at level 2 with cvc5.
