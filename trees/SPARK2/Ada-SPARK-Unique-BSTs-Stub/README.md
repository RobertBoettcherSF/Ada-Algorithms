# Ada-SPARK-Unique-BSTs-Stub

A bounded SPARK implementation of compute the number of structurally unique bsts.

## Verification

```text
make test
make prove
```

The package uses `SPARK_Mode => On`; proof is run at level 2 with cvc5.
