# Ada-SPARK-Trim-BST-Stub

A bounded SPARK implementation of trim a bounded bst to a value range.

## Verification

```text
make test
make prove
```

The package uses `SPARK_Mode => On`; proof is run at level 2 with cvc5.
