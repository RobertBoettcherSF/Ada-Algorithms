# Ada-SPARK-Symmetric-Tree

Bounded Ada/SPARK implementation of the Symmetric Tree algorithm.

## Verification

```sh
make test
make prove
```

The implementation uses `pragma SPARK_Mode (On)` and fixed-size bounded storage.
