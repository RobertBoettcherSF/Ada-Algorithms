# Ada-SPARK-Construct-Binary-Tree-From-Preorder-And-Inorder-Lite

Construct a small binary tree from preorder and inorder arrays. The implementation is bounded to 16 nodes, uses arrays rather than access types, enables `SPARK_Mode (On)`, and includes a level-2 CVC5 proof target.

```text
make test
make prove
```
