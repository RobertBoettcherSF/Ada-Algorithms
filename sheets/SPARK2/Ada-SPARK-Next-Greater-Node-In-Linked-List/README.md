# Ada-SPARK-Next-Greater-Node-In-Linked-List

A bounded Ada/SPARK implementation of bounded next-greater-node scan for a list representation with inputs limited to at most 32 elements (8 rows/columns for the matrix).

Run `make test` for executable checks and `make prove` for level-2 CVC5 verification.
