# Ada-SPARK-Secant-Method

A bounded secant step for the linear residual f(x) = x - target. The package is bounded and compiled in SPARK mode.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks treated as errors.
