# Ada-SPARK-Binary-Tree-Min-Depth

Bounded Ada/SPARK implementation of the Binary Tree Min Depth algorithm.

## Verification

```sh
make test
make prove
```

The implementation uses `pragma SPARK_Mode (On)` and fixed-size bounded storage.
