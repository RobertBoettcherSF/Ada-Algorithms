# Ada-SPARK-Copy-List-With-Random-Pointer-Lite

A bounded SPARK array-backed random-pointer list copy without access types.

All storage is bounded to 16 entries and uses SPARK_Mode (On).

## Checks

```text
make test
make prove
```
