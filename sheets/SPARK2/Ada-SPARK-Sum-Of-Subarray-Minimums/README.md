# Ada-SPARK-Sum-Of-Subarray-Minimums

A bounded Ada/SPARK implementation of bounded sum of subarray minimums with inputs limited to at most 32 elements (8 rows/columns for the matrix).

Run `make test` for executable checks and `make prove` for level-2 CVC5 verification.
