# Longest Common Subsequence in Ada/SPARK

Bounded LCS length (DP). Companion: [Ada-Longest-Common-Subsequence](https://github.com/RobertBoettcherSF/Ada-Longest-Common-Subsequence).

Proof bar: `make prove` = Level 2 full (Pareto). Raise with `--level=3|4` when free.

`make` · `make test` · `make prove`

Shipped with `make test` green. Run `make prove` (Pareto L2+) when the prove queue is free.

