# Ada-SPARK-Combination-Sum-II

Bounded distinct-part combination-sum counts up to twelve. The implementation is bounded and compiled with `SPARK_Mode => On`.

## Checks

```text
make test
make prove
```

The proof command runs level-2 GNATprove with cvc5, warnings treated as errors, and checks treated as errors.
