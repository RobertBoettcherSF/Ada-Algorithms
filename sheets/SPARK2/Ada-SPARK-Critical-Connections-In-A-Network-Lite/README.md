# Ada-SPARK-Critical-Connections-In-A-Network-Lite

Bounded SPARK bridge counting by edge-removal reachability.

The implementation uses fixed-size arrays (`n <= 16`) and is checked with GNATprove at level 2 using cvc5.
