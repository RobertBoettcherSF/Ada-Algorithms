# Ada-SPARK-Find-Mode-In-BST

Find a mode in a bounded array-backed BST. The implementation is bounded to 16 nodes, uses arrays rather than access types, enables `SPARK_Mode (On)`, and includes a level-2 CVC5 proof target.

```text
make test
make prove
```
