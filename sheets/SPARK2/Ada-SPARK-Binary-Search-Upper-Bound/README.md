# Ada-SPARK-Binary-Search-Upper-Bound

A bounded, SPARK-proved upper-bound search for a sorted six-element array. The result is the first position whose value is strictly greater than the target, or `Length + 1` when no such position exists.
