# Ada-SPARK-Swapping-Nodes-In-A-Linked-List

A bounded SPARK array list that swaps two node positions without access types.

All storage is bounded to 16 entries and uses SPARK_Mode (On).

## Checks

```text
make test
make prove
```
