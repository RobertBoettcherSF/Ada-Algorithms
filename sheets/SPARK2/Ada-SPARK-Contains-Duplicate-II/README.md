# Ada-SPARK-Contains-Duplicate-II

Bounded SPARK nearby-duplicate detector with distance two. Arrays are fixed at eight elements for bounded verification.

Run `make test` and `make prove` with the Ada/SPARK toolchain.
