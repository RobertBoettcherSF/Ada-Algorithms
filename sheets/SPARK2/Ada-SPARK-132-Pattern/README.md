# Ada-SPARK-132-Pattern

A bounded Ada/SPARK implementation of bounded 132-pattern detection with inputs limited to at most 32 elements (8 rows/columns for the matrix).

Run `make test` for executable checks and `make prove` for level-2 CVC5 verification.
