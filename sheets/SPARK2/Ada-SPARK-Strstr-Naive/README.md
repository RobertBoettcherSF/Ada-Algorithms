# Ada-SPARK-Strstr-Naive

Bounded naive substring search with a zero result when absent.

Build and run tests with `make test`. Run the full SPARK Level 2 proof with `make prove` after sourcing `/home/box/deps/spark/env.sh`.
