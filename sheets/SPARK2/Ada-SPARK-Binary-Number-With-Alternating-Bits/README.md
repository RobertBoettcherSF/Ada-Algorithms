# Ada-SPARK-Binary-Number-With-Alternating-Bits

A bounded eight-bit predicate for the two alternating-bit byte patterns.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks treated as errors.
