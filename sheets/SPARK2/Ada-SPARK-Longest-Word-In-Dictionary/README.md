# Ada-SPARK-Longest-Word-In-Dictionary

Find the longest word whose every prefix occurs in a bounded dictionary. Implemented as a small bounded Ada SPARK example with `SPARK_Mode => On`.

```sh
make test
make prove
```
