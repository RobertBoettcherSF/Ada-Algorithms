# Ada-SPARK-Count-Triplets-That-Can-Form-Two-Arrays-Of-Equal-XOR

A bounded SPARK model for the three-value equal-XOR triplet predicate.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks as errors.
