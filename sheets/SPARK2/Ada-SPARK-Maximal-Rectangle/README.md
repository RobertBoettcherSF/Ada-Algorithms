# Ada-SPARK-Maximal-Rectangle

A bounded Ada/SPARK implementation of bounded maximal all-one rectangle search with inputs limited to at most 32 elements (4 rows/columns for the matrix).

Run `make test` for executable checks and `make prove` for level-2 CVC5 verification.
