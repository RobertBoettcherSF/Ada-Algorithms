# Ada-SPARK-Matrix-Chain-Multiplication

A bounded three-matrix chain cost calculation in SPARK.

`make test` builds and runs the bounded test suite. `make prove` runs a full SPARK Level 2 proof with CVC5.
