# Ada-SPARK-First-Unique-Character

A bounded search over eight characters returning the first unique position, or zero.

`make test` runs the executable checks; `make prove` runs level-2 GNATprove with cvc5.
