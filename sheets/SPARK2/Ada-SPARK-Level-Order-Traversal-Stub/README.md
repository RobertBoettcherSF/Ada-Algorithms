# Ada-SPARK-Level-Order-Traversal-Stub

Bounded Ada/SPARK implementation of the Level Order Traversal Stub algorithm.

## Verification

```sh
make test
make prove
```

The implementation uses `pragma SPARK_Mode (On)` and fixed-size bounded storage.
