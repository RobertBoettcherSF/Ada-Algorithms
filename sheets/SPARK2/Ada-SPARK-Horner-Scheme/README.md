# Ada-SPARK-Horner-Scheme

Horner evaluation of a bounded cubic polynomial. The package is bounded and compiled in SPARK mode.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks treated as errors.
