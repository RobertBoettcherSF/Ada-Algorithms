# Ada-SPARK-Neighboring-Bitwise-XOR

A bounded SPARK check for the cyclic neighboring-XOR consistency condition.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks as errors.
