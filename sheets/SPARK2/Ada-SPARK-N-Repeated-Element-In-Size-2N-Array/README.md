# Ada-SPARK-N-Repeated-Element-In-Size-2N-Array

A bounded SPARK stub for recognizing a repeated value in a 2N-sized array.

Build and run tests with `make test`; run the level-2 SPARK proof with `make prove`.
