# Ada-SPARK-Product-Of-Array-Except-Self

Bounded SPARK product-of-array-except-self calculation. Arrays are fixed at eight elements for bounded verification.

Run `make test` and `make prove` with the Ada/SPARK toolchain.
