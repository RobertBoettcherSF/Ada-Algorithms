# Ada-SPARK-Find-The-Duplicate-Number

Bounded SPARK duplicate-number search. Arrays are fixed at eight elements for bounded verification.

Run `make test` and `make prove` with the Ada/SPARK toolchain.
