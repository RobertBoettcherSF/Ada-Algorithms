# Ada/SPARK Pearson Hashing

A compact Pearson-style hash with a deliberately tiny 16-entry permutation table.
The bounded table makes the mechanics easy to inspect and prove.

Run `make test` and `make prove` (Level 2, cvc5, warnings as errors).
