# Ada-SPARK-Minimum-Depth-Of-Binary-Tree

The minimum root-to-leaf depth for a bounded binary tree.

Bounded to 15 addressable nodes with fixed-size array-backed storage and `SPARK_Mode (On)`.

```sh
make test
make prove
```
