# Ada-SPARK-Find-The-Original-Array-Of-Prefix-XOR

A bounded SPARK reconstruction of a four-element array from prefix XOR values.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks as errors.
