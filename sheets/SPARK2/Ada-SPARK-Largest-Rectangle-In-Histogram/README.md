# Ada-SPARK-Largest-Rectangle-In-Histogram

A bounded Ada/SPARK implementation of bounded largest-rectangle search over a histogram with inputs limited to at most 32 elements (8 rows/columns for the matrix).

Run `make test` for executable checks and `make prove` for level-2 CVC5 verification.
