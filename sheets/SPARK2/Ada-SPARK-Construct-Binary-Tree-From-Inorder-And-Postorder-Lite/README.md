# Ada-SPARK-Construct-Binary-Tree-From-Inorder-And-Postorder-Lite

Construct a small binary tree from inorder and postorder arrays. The implementation is bounded to 16 nodes, uses arrays rather than access types, enables `SPARK_Mode (On)`, and includes a level-2 CVC5 proof target.

```text
make test
make prove
```
