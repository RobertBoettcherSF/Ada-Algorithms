# Ada-SPARK-Longest-Palindromic-Substring

A bounded search for the length of the longest palindromic substring in seven characters.

`make test` runs the executable checks; `make prove` runs level-2 GNATprove with cvc5.
