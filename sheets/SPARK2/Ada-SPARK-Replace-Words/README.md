# Ada-SPARK-Replace-Words

Replace each bounded token by its shortest matching dictionary root. Implemented as a small bounded Ada SPARK example with `SPARK_Mode => On`.

```sh
make test
make prove
```
