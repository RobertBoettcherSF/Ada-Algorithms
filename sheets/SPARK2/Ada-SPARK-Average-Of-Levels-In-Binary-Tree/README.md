# Ada-SPARK-Average-Of-Levels-In-Binary-Tree

Compute a bounded binary-tree level average. The implementation is bounded to 16 nodes, uses arrays rather than access types, enables `SPARK_Mode (On)`, and includes a level-2 CVC5 proof target.

```text
make test
make prove
```
