# Ada-SPARK-Trim-A-Binary-Search-Tree

A bounded array-backed tree exercise. Values are clamped to an inclusive BST range without access types.

```sh
source /home/box/deps/spark/env.sh
make test
make prove
```
