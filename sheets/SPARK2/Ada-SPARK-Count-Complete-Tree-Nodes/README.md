# Ada-SPARK-Count-Complete-Tree-Nodes

Count nodes in a bounded complete array-backed tree. The implementation is bounded to 16 nodes, uses arrays rather than access types, enables `SPARK_Mode (On)`, and includes a level-2 CVC5 proof target.

```text
make test
make prove
```
