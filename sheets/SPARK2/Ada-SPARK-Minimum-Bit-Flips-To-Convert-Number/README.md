# Ada-SPARK-Minimum-Bit-Flips-To-Convert-Number

A bounded 8-bit SPARK implementation of the minimum bit-flip distance.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks as errors.
