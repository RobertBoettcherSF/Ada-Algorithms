# Ada-SPARK-Uncommon-Words-From-Two-Sentences

A bounded SPARK stub for detecting a word present only on the left.

Build and run tests with `make test`; run the level-2 SPARK proof with `make prove`.
