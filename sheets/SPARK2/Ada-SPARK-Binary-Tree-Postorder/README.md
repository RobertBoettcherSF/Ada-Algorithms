# Ada-SPARK-Binary-Tree-Postorder

An array-backed postorder traversal with a bounded work stack.

Bounded to 15 addressable nodes with fixed-size array-backed storage and `SPARK_Mode (On)`.

```sh
make test
make prove
```
