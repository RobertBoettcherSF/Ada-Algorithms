-- celp.gpr
project Celp is
   for Source_Dirs use (".");
   for Object_Dir use ".";
   for Exec_Dir use ".";
   for Main use ("tests.adb");
   
   package Compiler is
      for Switches ("Ada") use ("-g", "-O2", "-gnata"); -- Enable assertions
   end Compiler;
end Celp;
