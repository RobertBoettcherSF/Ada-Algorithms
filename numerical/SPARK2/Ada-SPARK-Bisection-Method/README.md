# Ada-SPARK-Bisection-Method

A bounded integer bisection search for a square root. The package is bounded and compiled in SPARK mode.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks treated as errors.
