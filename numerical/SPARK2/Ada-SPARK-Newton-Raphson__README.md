# Ada-SPARK-Newton-Raphson

A bounded integer Newton-Raphson square-root iteration. The package is bounded and compiled in SPARK mode.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks treated as errors.
