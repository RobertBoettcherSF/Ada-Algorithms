# Ada-SPARK-String-To-Integer-Atoi

A bounded decimal parser for one to six digits, represented without unbounded strings.

`make test` runs the executable checks; `make prove` runs level-2 GNATprove with cvc5.
