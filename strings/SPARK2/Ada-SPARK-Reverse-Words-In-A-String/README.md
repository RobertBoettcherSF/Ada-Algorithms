# Ada-SPARK-Reverse-Words-In-A-String

A bounded word-order reversal for three three-letter words separated by spaces (11 characters).

`make test` runs the executable checks; `make prove` runs level-2 GNATprove with cvc5.
