# Ada-SPARK-Count-The-Number-Of-Consistent-Strings

A bounded SPARK stub for counting symbols within an allowed range.

Build and run tests with `make test`; run the level-2 SPARK proof with `make prove`.
