# Ada-SPARK-Single-Number-II

A bounded SPARK scaffold for the single non-triplicate element in a five-value arrangement.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks treated as errors.
