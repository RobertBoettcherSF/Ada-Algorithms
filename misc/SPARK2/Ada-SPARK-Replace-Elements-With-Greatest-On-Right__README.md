# Ada-SPARK-Replace-Elements-With-Greatest-On-Right

Bounded SPARK example: replace each value with the right-side maximum.

`make test` builds and runs the checks; `make prove` runs level-2 CVC5 proofs.
