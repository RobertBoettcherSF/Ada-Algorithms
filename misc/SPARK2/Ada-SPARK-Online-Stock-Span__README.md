# Ada-SPARK-Online-Stock-Span

A bounded Ada/SPARK implementation of bounded online stock-span calculation with inputs limited to at most 32 elements (8 rows/columns for the matrix).

Run `make test` for executable checks and `make prove` for level-2 CVC5 verification.
