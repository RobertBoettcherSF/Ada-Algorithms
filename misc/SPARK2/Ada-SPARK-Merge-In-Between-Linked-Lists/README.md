# Ada-SPARK-Merge-In-Between-Linked-Lists

A bounded SPARK array list that replaces an inclusive range with a middle value.

All storage is bounded to 16 entries and uses SPARK_Mode (On).

## Checks

```text
make test
make prove
```
