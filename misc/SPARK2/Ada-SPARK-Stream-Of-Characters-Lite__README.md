# Ada-SPARK-Stream-Of-Characters-Lite

Query a bounded recent character stream against a small dictionary. Implemented as a small bounded Ada SPARK example with `SPARK_Mode => On`.

```sh
make test
make prove
```
