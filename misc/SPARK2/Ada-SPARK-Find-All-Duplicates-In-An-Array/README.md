# Ada-SPARK-Find-All-Duplicates-In-An-Array

Bounded SPARK duplicate finder; zero marks a non-duplicate slot. Arrays are fixed at eight elements for bounded verification.

Run `make test` and `make prove` with the Ada/SPARK toolchain.
