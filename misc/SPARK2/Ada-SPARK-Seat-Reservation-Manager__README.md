# Ada-SPARK-Seat-Reservation-Manager

A bounded first-free-seat query over sixteen seats.

`make test` builds and runs bounded tests. `make prove` runs full level-2 SPARK proof with cvc5 and warnings/checks treated as errors.
