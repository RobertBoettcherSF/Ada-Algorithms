# Ada-SPARK-Generate-Parentheses

A bounded three-pair exercise returning the canonical first balanced sequence.

`make test` runs the executable checks; `make prove` runs level-2 GNATprove with cvc5.
