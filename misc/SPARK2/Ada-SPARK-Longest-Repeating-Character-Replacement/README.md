# Ada-SPARK-Longest-Repeating-Character-Replacement

Finds the longest bounded window made uniform with at most one replacement.

The implementation is deliberately bounded to eight-element inputs so every check is suitable for SPARK Level 2 proof.
