# Ada-SPARK-Last-Stone-Weight-II

A bounded SPARK implementation for six stones (weights 0..10), enumerating the 32 complementary subset pairs.

`make test` builds and runs checks. `make prove` runs level-2 SPARK proof with cvc5.
