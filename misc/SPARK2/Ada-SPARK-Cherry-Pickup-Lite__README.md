# Ada-SPARK-Cherry-Pickup-Lite

A bounded single-route Cherry Pickup lite model with one cherry per step (0 <= steps <= 16).

The implementation is deliberately bounded for a compact Ada/SPARK exercise.
