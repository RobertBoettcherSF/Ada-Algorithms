# Ada-SPARK-XOR-Operation-In-An-Array

A bounded SPARK implementation of XOR over `Start + 2 * I` for at most eight items.

- `make test` builds and runs assertions.
- `make prove` runs level-2 CVC5 proofs with warnings and checks as errors.
