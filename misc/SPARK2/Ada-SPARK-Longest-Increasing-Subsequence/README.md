# Ada-SPARK-Longest-Increasing-Subsequence

Bounded Ada/SPARK longest-increasing-subsequence dynamic program.

`make test` runs the bounded test suite; `make prove` runs SPARK level-2 proof with cvc5.
