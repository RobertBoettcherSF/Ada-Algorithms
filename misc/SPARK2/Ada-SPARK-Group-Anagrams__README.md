# Ada-SPARK-Group-Anagrams

A bounded SPARK exercise grouping three five-character words by anagram equivalence.

`make test` runs the executable checks; `make prove` runs level-2 GNATprove with cvc5.
