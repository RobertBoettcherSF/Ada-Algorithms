# Ada-SPARK-Maximum-Subarray-Circular

Bounded SPARK maximum circular-subarray calculation. Arrays are fixed at eight elements for bounded verification.

Run `make test` and `make prove` with the Ada/SPARK toolchain.
